{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "dependencies": {
    "@storybook/cli": "^7.3.2"
  },
  "devDependencies": {
    "turbo": "^1.10.13"
  }
}
